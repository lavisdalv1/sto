import { Image } from '@chakra-ui/react';

export const ArtDentIcon = (props) => (
  <Image
    src="/images/artDent.png"
    width={{ base: '55px', md: '65px' }}
    height={{ base: '55px', md: '65px' }}
    {...props}
  />
);

export default ArtDentIcon;
